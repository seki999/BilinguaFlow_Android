<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!--
         Required on Android 11+ (API 30) package visibility so SpeechRecognizer.isRecognitionAvailable()
         and createSpeechRecognizer()/startListening() can actually see and bind to the device's
         RecognitionService implementation. SpeechRecognizer binds a Service (RecognitionService.SERVICE_INTERFACE),
         not an Activity, so the queries element must target "android.speech.RecognitionService" —
         "android.speech.action.RECOGNIZE_SPEECH" is only for the separate, Activity-based
         RecognizerIntent UI flow (startActivityForResult), which this app does not use.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".BilinguaFlowApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BilinguaFlow">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BilinguaFlow"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
